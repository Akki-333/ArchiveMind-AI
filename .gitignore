# Environment variables (CRITICAL: Never commit this)
.env
.env.local
.env.example

# Development-only JWT signing key, generated on first run when JWT_SECRET is
# unset. Local to each machine; production sets JWT_SECRET explicitly.
.jwt_secret.dev

# Engineering notes. Useful locally, not part of the shipped repository.
CLAUDE.md

# Python
__pycache__/
*.py[cod]
*$py.class
venv/
env/
.venv/
pip-log.txt

# Node / Javascript
node_modules/
dist/
build/
npm-debug.log*
yarn-debug.log*

# OS Files
.DS_Store
Thumbs.db

# IDEs / Editors
.vscode/
.idea/

# Logs and DBs
*.log
*.sqlite3
*.db

# Temporary/Debug Files (Do not push to Github)
debug_output.txt
debug_*.py
download_*.py
poc/

# Note: `test_*.py` used to be ignored here, which meant the repository
# structurally could not hold a test suite. Real tests live in backend/tests/.

# Test and build artefacts
.pytest_cache/
.coverage
htmlcov/
coverage/
